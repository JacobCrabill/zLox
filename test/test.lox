fun fib(n) {
  if (n < 2) return n;
  return fib(n - 2) + fib(n - 1);
}

var n = 35;

var start = clock();
var res = fib(n);
var stop = clock();
print "Elapsed time in seconds: " + str(stop - start);
print "Answer: " + str(res);
